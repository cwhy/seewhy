# Experiment Workflow — AR-Recall

Recall-gen made one image one token. This project takes the item apart into an
autoregressive stream of `(label, position, value)` triples, so that binding a
label to content spread over thousands of tokens is the thing being measured.

For algorithmic details (model architecture, loss, key hyperparameters) see
[concepts.md](concepts.md).

---

## Directory Layout

```
projects/ar-recall/
├── workflow.md             # This file
├── concepts.md             # Algorithm details, metrics, key findings
├── experimentsN.py         # One file per experiment; numbered sequentially
├── results.jsonl           # Append-only results log — one JSON object per line
├── params_expN.pkl         # Saved model params after each run (gitignored)
├── history_expN.pkl        # Per-epoch training history (gitignored)
├── logs/                   # Experiment and runner logs (gitignored)
├── reports/*.md            # Intermediate findings, one per experiment
├── paper/                  # The deliverable — see "Reports and the paper"
│   ├── main.typ            # Title, abstract, the #include list, bibliography
│   ├── template.typ        # Show rule and helpers (#fig #callout #gloss #todo)
│   ├── refs.bib
│   ├── sections/*.typ      # Hand-written prose, one file per section
│   ├── figures/*.typ       # Generated by gen_report.py — committed
│   ├── assets/*.json       # Generated figure data — committed
│   ├── .lint-allow         # Prose numbers that are legitimately not results
│   └── .build.json         # Hashes + URLs from the last publish — committed
├── lib/
│   ├── __init__.py
│   ├── mixers.py           # Causal delta rule (chunkwise) and attention
│   └── figures.py          # Figure specs for both tiers (gribouille, not drawing code)
└── scripts/
    ├── run_experiments.py  # Launch and manage experiment runs  ← always use this
    ├── poll_result.py      # Wait for / display results
    ├── gen_report.py       # Regenerate figures into the paper tree
    ├── test_mixers.py      # Correctness of the mixers against a scan reference
    ├── bench_mixers.py     # Memory and step time against sequence length
    └── tmp/                # Throwaway / generator scripts (not committed)
```

`figures/`, `assets/` and `.build.json` are **committed**, which the nested
`paper/.gitignore` arranges: without the figure data a fresh clone cannot
rebuild the PDF, and without `.build.json` in git, `--status` on the Mac sees
no prior state (publishing happens on the GPU box, so that is where the file
would otherwise stay).

---

## Invariant Rules

These apply to every session without exception:

1. **Always `uv run python`** — never raw `python` or `python3`. The `.venv`
   has CUDA-specific packages; raw python may resolve a different interpreter.

2. **Always use `run_experiments.py`** to launch experiments — never call
   `uv run python experimentsN.py` directly. The runner handles log paths,
   GPU assignment, and the results table.

3. **Throwaway scripts go in `scripts/tmp/`** — never `python -c "..."` inline.
   Write to `scripts/tmp/<name>.py`, then run with
   `uv run python projects/ar-recall/scripts/tmp/<name>.py`.

4. **No bash polling loops** — to check progress, read the log file once or
   write a Python script. Never `while true; do ...; sleep N; done`.

---

## Running Experiments

```bash
# Fire and forget — detach to background, return immediately
uv run python projects/ar-recall/scripts/run_experiments.py --bg exp1

# With auto-viz after completion
uv run python projects/ar-recall/scripts/run_experiments.py --bg --viz scripts/gen_viz_exp1.py exp1

# Sequential blocking (safe — no XLA contention)
uv run python projects/ar-recall/scripts/run_experiments.py exp1 exp2

# Parallel across GPUs
uv run python projects/ar-recall/scripts/run_experiments.py --parallel exp1 exp2

# Pin to a specific GPU
uv run python projects/ar-recall/scripts/run_experiments.py --gpu 1 exp1
```

Logs always go to `projects/ar-recall/logs/{exp_name}.log`.

---

## Monitoring

```bash
# Check progress of a running experiment
tail -20 projects/ar-recall/logs/exp1.log

# Poll until result appears in results.jsonl
uv run python projects/ar-recall/scripts/poll_result.py exp1
uv run python projects/ar-recall/scripts/poll_result.py --all
```

---

## JAX Performance

### Use `lax.scan` for the batch loop

Replace `for b in range(num_batches)` with `jax.lax.scan`. This eliminates
thousands of Python→XLA dispatch calls per epoch and lets XLA fuse the full
epoch as one computation.

```python
def make_epoch_fn(optimizer):
    def scan_body(carry, inputs):
        params, opt_state = carry
        batch, key = inputs
        (loss, aux), grads = jax.value_and_grad(loss_fn, has_aux=True)(
            params, batch, key
        )
        updates, new_opt_state = optimizer.update(grads, opt_state, params)
        return (optax.apply_updates(params, updates), new_opt_state), (loss, *aux)

    def epoch_fn(params, opt_state, Xs_batched, epoch_key):
        num_batches = Xs_batched.shape[0]
        keys = jax.vmap(lambda b: jax.random.fold_in(epoch_key, b))(
            jnp.arange(num_batches)
        )
        (params, opt_state), stats = jax.lax.scan(
            scan_body, (params, opt_state), (Xs_batched, keys)
        )
        return params, opt_state, *[s.mean() for s in stats]

    return jax.jit(epoch_fn)
```

Pre-batch data before calling:
```python
Xs_batched = Xs[:num_batches * BATCH_SIZE].reshape(num_batches, BATCH_SIZE, -1)
```

### GPU isolation

Never launch two JAX experiments on the same GPU simultaneously. XLA
compilations from two processes compete for memory and can OOM. Use
`run_experiments.py` sequential mode or `--parallel` (which sets
`CUDA_VISIBLE_DEVICES` per process).

### First epoch is slow — JIT compilation

Epoch 0 includes XLA compilation time and may be 10–20× slower than
subsequent epochs. Do not judge speed from epoch 0.

### Don't close over large arrays in `@jax.jit`

If a `@jax.jit` function closes over a large array, JAX embeds it as a
compile-time constant and allocates it on the GPU at compile time. This
silently OOMs when the function is recompiled for a new shape or after
prior allocations have fragmented memory.

```python
# Bad — E_b (~245 MB) captured as constant, OOMs on recompile
@jax.jit
def epoch(W, b, state):
    (W, b), state = jax.lax.scan(step, ((W, b), state), (E_b, Y_b))[0]
    return W, b, state

# Good — pass as arguments
@jax.jit
def epoch(W, b, state, E_b, Y_b):
    (W, b), state = jax.lax.scan(step, ((W, b), state), (E_b, Y_b))[0]
    return W, b, state
```

---

## Experiment File Structure

Every experiment file follows the same layout:

```
module docstring    — hypothesis, what changed, usage line
hyperparameters     — UPPER_SNAKE_CASE constants at module top
precomputed consts  — JAX arrays that don't change per run
utilities           — n_params, append_result, any project-specific helpers
init_params()
encode() / forward()
loss_fn()
make_epoch_fn()     — returns JIT-compiled epoch fn
eval_metrics()      — @jax.jit accuracy / metric functions
train()             — epoch loop with periodic viz; returns (params, history, elapsed)
__main__            — load data → init → train → visualize → append_result
```

Hyperparameters go at module level in `UPPER_SNAKE_CASE` so they are visible
at a glance and easy to diff between experiments.

---

## Results Logging

### Skip-if-done

Check at the top of every `__main__` whether the experiment is already in the
output JSONL and exit early. Makes batch runners idempotent after a crash:

```python
done = set()
if JSONL.exists():
    with open(JSONL) as f:
        for line in f:
            try: done.add(json.loads(line).get("experiment"))
            except Exception: pass
if EXP_NAME in done:
    logging.info(f"{EXP_NAME} already done — skipping")
    exit(0)
```

### JSONL deduplication

Two concurrent processes both read the `done` set before either has written,
then both write the same rows. After any crash or concurrent run, deduplicate
by key before analysis:

```python
seen, unique = set(), []
for r in rows:
    k = r.get("experiment")   # or whatever the natural key is
    if k not in seen:
        seen.add(k)
        unique.append(r)
```

### Append one JSON object per experiment to `results.jsonl`:

```python
JSONL = Path(__file__).parent / "results.jsonl"

def append_result(row: dict):
    with open(JSONL, "a") as f:
        f.write(json.dumps(row) + "\n")
```

Every row must include: all hyperparameters, `n_params`, `time_s`, `epochs`,
final metric values, and per-epoch curve lists. Curves are cheap to store and
invaluable for re-plotting without re-running.

---

## Saving and Reloading Params

Save params at the end of every run:

```python
with open(Path(__file__).parent / f"params_{EXP_NAME}.pkl", "wb") as f:
    pickle.dump({k: np.array(v) for k, v in params.items()}, f)

with open(Path(__file__).parent / f"history_{EXP_NAME}.pkl", "wb") as f:
    pickle.dump(history, f)
```

To reload in a script:

```python
with open("projects/ar-recall/params_exp1.pkl", "rb") as f:
    params = {k: jnp.array(v) for k, v in pickle.load(f).items()}
```

Both pkl files are gitignored. `results.jsonl` is the only committed output.

---

## Visualization

Shared visualization functions live in `lib/viz.py`. Do not inline
visualization code in experiment files — it diverges and is hard to keep
consistent.

```python
from lib.viz import save_learning_curves, save_reconstruction_grid
```

Format guide:
- **SVG** for learning curves, line plots, anything vector-based
- **PNG** (dpi=150) for pixel images — reconstructions, heatmaps, sample grids

Always log the returned URL:

```python
url = save_matplotlib_figure("exp1_learning_curves", fig, format="svg")
logging.info(f"  learning curves → {url}")
```

### Post-training regeneration

```bash
uv run python projects/ar-recall/scripts/gen_viz_exp1.py
```

Reads `params_exp1.pkl` and `history_exp1.pkl` from disk and regenerates
all plots without re-running the experiment.

---

## Reports and the paper

Two tiers, with different contracts. They are not long and short versions of
each other.

| | `reports/*.md` | `paper/` |
|---|---|---|
| what | lab notebook, one per experiment | the deliverable |
| audience | you, next week | someone who has never heard of the project |
| assumes | all project context | programming and statistics, no ML |
| when | as results land | scaffolded at project start, republished throughout |
| format | markdown → R2 HTML | Typst → PDF + viewer page |

### Intermediate reports — markdown

Terse, disposable, written while the result is fresh. Plots linked as R2 URLs,
never base64 (1+ MB vs ~10 KB, and unreadable diffs).

```python
import sys
sys.path.insert(0, str(Path(__file__).parent.parent.parent.parent))
from shared_lib.media import save_matplotlib_figure
from shared_lib.report import save_report

url = save_matplotlib_figure("ar-recall_my_plot", fig, dpi=150)
plt.close(fig)
report_url = save_report("ar-recall_report_<name>", md_string)   # ![plot](url) inside
```

Publishing a markdown **string** with `save_report()` needs no local file. Use
`save_report_file()` for a hand-written `.md`. Do not pass `title=` to either —
it injects a duplicate `<h1>` over the markdown's own `# heading`.

### Figures shared between both tiers

`lib/figures.py` returns `Figure` objects from `shared_lib.typst_plot` — data
plus a declarative gribouille spec, not drawing code. The same object feeds
either tier, so a corrected axis or baseline propagates to both:

```python
from shared_lib.typst_plot import line_chart, write_figures
from shared_lib.typst_report import save_figure

fig = line_chart("accuracy", data, x="step", y="acc", colour="run")

write_figures(PROJECT / "paper", [fig])      # into the paper tree
url = save_figure(fig)                        # standalone image for a markdown report
```

`save_figure` compiles the figure on its own with the data inlined and returns
an R2 URL. SVG carries text as outlines, so it renders in an `<img>` with no
font available; it also grows with point count while PNG does not — past a few
hundred points per series, pass `fmt="png"`.

Genuinely-pixel figures (sample grids, reconstructions, attention maps) stay on
matplotlib. There is no grammar-of-graphics spec for "show me the pixels".

### The paper

A directory of small files: `sections/*.typ` hand-written, `figures/*.typ` and
`assets/*` generated, compiled by `typst-py` and pushed to R2. Updating is
partial by nature — rewrite one section, recompile, publish.

```bash
uv run python -m shared_lib.publish projects/ar-recall/paper --status    # what moved
uv run python -m shared_lib.publish projects/ar-recall/paper --check     # lint
uv run python -m shared_lib.publish projects/ar-recall/paper --section 06-results
uv run python -m shared_lib.publish projects/ar-recall/paper --preview   # → paper/out.pdf
uv run python -m shared_lib.publish projects/ar-recall/paper --stable    # publish
```

Run `gen_report.py` first if results changed — the publish command compiles
what is on disk and does not regenerate figures.

**Always `--stable`.** The paper is republished as work proceeds, so its link
gets shared before it is finished. A date-prefixed key would leave every link
already shared pointing at a stale version, which is worse than a dead one
because it still renders. `--stable` overwrites one key with a five-minute
cache lifetime.

Only the PDF is uploaded; the shared link is a ~3 KB page that renders it with
pdf.js from a CDN.

Obligations per section, and the audience contract, are in
[`../TEMPLATES/paper_checklist.md`](../TEMPLATES/paper_checklist.md). The
scaffolded sections carry the same list as comments. `#todo[...]` marks
unwritten passages: a red box while `status: "draft"`, a hard compile error
once `main.typ` says `status: "final"`.

---

## Diagnostic Scripts (`scripts/tmp/`)

One-off helpers live in `scripts/tmp/` and are not committed. Examples:

| Script | Purpose |
|--------|---------|
| `inspect_expN.py` | Print metrics / shapes for a saved run |
| `compare_exps.py expA expB` | Side-by-side metric comparison |
| `mk_expN.py` | Generator that patches a parent experiment into a new one |

Run all with `uv run python projects/ar-recall/scripts/tmp/<name>.py [args]`.


## Project-specific notes

**The delta rule here is chunkwise, not a scan.** `lib/mixers.kda_chunkwise`
solves each chunk's corrections with one triangular solve instead of stepping
through it, which measured ~10x faster than the scan at every length tested and
is what makes a 17,640-token episode affordable. `kda_causal` is the scan, kept
as the reference the chunkwise form is tested against — do not delete it.

**The kernel has a precondition.** It divides by the within-chunk cumulative
decay, so it needs the decay gate near 1. At the project's initialisation
(a ~ 0.994) chunk 128 is comfortable; it holds to a ~ 0.9 and breaks below about
a ~ 0.4 where the cumulative product underflows float32. Chunk 64 tolerates
roughly twice as much forgetting. `scripts/test_mixers.py` measures this rather
than assuming it, and a run whose gates learn to forget fast needs a smaller
chunk.

**Exactness claims belong in float64.** The chunkwise form and the scan differ by
~5e-4 in float32 purely from summation order; in float64 they agree to 8e-16.
A float32 threshold tight enough to be meaningful will fail for the wrong reason.

**Memory is `batch x tokens`.** Every configuration that fits sits near 10 GiB
and the next batch up OOMs on a 24 GiB card, so the budget is about 35,000
token-samples per step whatever the shape. Buy diversity back with gradient
accumulation or both GPUs, not with a bigger batch.

**Attention cannot run the long configurations here.** cuDNN will not build a
flash plan for these shapes and needs bf16 besides, so what runs is the XLA path
at O(N^2) memory, which OOMs past ~1,500 tokens. Keeping attention as a control
past that length needs a hand-written kernel.

**`sys.path` order matters.** The repo root must be appended, not inserted — the
GPU box has an untracked `datasets.py` at its root that otherwise shadows the
HuggingFace `datasets` package. `lib` is also this project's package, so
recall-gen's `lib/core.py` is loaded by path rather than by adding it to the
path.
