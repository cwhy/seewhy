#import "/template.typ": *

// OBLIGATIONS
//  - The acceptance test for this section: a competent stranger could
//    reimplement the work from it alone, without reading the repo.
//  - Model: equations or pseudocode. Not prose describing code.
//  - Loss: written out, with what is being summed over.
//  - A complete hyperparameter table — every value that would change the
//    result. #kv is for a handful; a real table for the full set.
//  - Seeds and repeats: how many, and whether reported numbers are single
//    runs or aggregates. If single, say so plainly.
//  - Hardware, wall-clock, parameter count.
//  - #notation for the symbol table, so section 6 can use symbols freely.

= Methodology

#todo[
  Model and loss, as equations or pseudocode.
]

== Notation

#notation(
  // ($N$, [number of support examples per episode]),
)

== Training setup

#todo[
  Hyperparameter table, seeds, hardware, wall-clock, parameter count.
]
