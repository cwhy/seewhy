#import "/template.typ": *

// OBLIGATIONS
//  - Its own section, not a hedge buried in the conclusion. Much of what this
//    repo produces is negative, and a negative result is only useful when its
//    scope is stated precisely.
//  - What was tried and did not work — including approaches abandoned before
//    they reached results.jsonl. That is the part a reader cannot reconstruct
//    from the repo, and the part that saves them repeating it.
//  - Where the result does NOT generalise: scale, dataset, architecture,
//    budget. Be specific about the boundary you actually tested.
//  - Known confounds you could not remove.

= Limitations and negative results

#todo[
  What did not work, what was not tested, and where the conclusion stops.
]
