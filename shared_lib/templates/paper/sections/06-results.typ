#import "/template.typ": *

// OBLIGATIONS
//  - Every metric carries its chance level or baseline IN THE SAME ROW. A
//    number without its reference point is not a result.
//  - Variance across seeds, or an explicit statement that a number is a single
//    run. "0.228" and "0.228 ± 0.04 over 8 seeds" are different claims.
//  - One figure per claim. Place with:
//        #fig(include "/figures/<name>.typ", caption: [...])
//    Figures are generated by the project's gen_report.py — never hand-edited.
//  - Report what happened, not what it means. Interpretation is section 7.
//  - Numbers here are cross-checked against results.jsonl by `--check`. If a
//    literal is legitimately not from results.jsonl, add it to .lint-allow
//    with a reason rather than leaving the warning to be ignored.

= Results

#todo[
  Results table: metric, value, chance/baseline, seeds.
]
