#import "/template.typ": *

// OBLIGATIONS
//  - This section is MANDATORY, and it is the one that makes the paper
//    readable by an outsider. The reader writes code and reads statistics;
//    they have never trained a neural network.
//  - Define every concept the paper later leans on, at first use. Attention,
//    in-context learning, autoregressive prediction, embedding, epoch, chance
//    level — whatever this particular paper uses. Use #gloss for the one-line
//    kind:  #gloss[induction head][a circuit that copies from an earlier match]
//  - Cover related work only insofar as it frames the question. A literature
//    survey is not the job; orienting the reader is.
//  - Cite with @key against refs.bib. `--check` reports unresolved keys.

= Background

#todo[
  What a reader needs to know before section 3 makes sense. Define terms as
  you introduce them.
]

== Related work

#todo[
  What has been tried, and what specifically remains open that this paper
  addresses.
]
